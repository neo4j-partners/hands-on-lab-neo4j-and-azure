# ============================================
# User Configuration
# ============================================
# These variables are configured manually by the user.

# Neo4j Connection
NEO4J_URI=                      # Neo4j connection URI (e.g., neo4j+s://xxx.databases.neo4j.io)
NEO4J_USERNAME=                 # Neo4j username
NEO4J_PASSWORD=                 # Neo4j password
NEO4J_VECTOR_INDEX_NAME=chunkEmbeddings  # Neo4j vector index name (default: chunkEmbeddings)
NEO4J_FULLTEXT_INDEX_NAME=search_chunks  # Neo4j fulltext index for chunk text search (default: search_chunks)
NEO4J_FULLTEXT_ENTITY_INDEX_NAME=search_entities  # Neo4j fulltext index for entity name search (default: search_entities)

# Embedding Configuration
EMBEDDING_DIMENSIONS=1536       # Embedding dimensions (1536 for ada-002, 3072 for text-embedding-3-large)

# ============================================
# Azure Configuration (managed by azd/setup_env.py)
# ============================================
# These variables are automatically populated by running: uv run setup_env.py
# They are synced from azd environment after running `azd up`.

AZURE_AI_AGENT_NAME=arches-agent
AZURE_AI_MODEL_NAME=            # Chat model deployment (default: gpt-4o, version: 2024-08-06)
AZURE_AI_EMBEDDING_NAME=        # Embedding model deployment (default: text-embedding-ada-002, version: 2)
AZURE_AI_PROJECT_ENDPOINT=      # Microsoft Foundry project endpoint
AZURE_OPENAI_ENDPOINT=          # Azure OpenAI endpoint
AZURE_TENANT_ID=                # Azure tenant ID
AZURE_RESOURCE_GROUP=           # Azure resource group

# ============================================
# Model Configuration
# ============================================
# Default models (deployed automatically by azd up):
#   Agent: gpt-4o
#   Embedding: text-embedding-ada-002
